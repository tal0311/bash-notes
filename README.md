# Notes/ToDo app for Shell

```
sudo apt update
git clone https://github.com/tal0311/bash-notes.git
cd bash-notes/
chmod +x note.sh
echo "alias note='~/bash-notes/note.sh'" >> ~/.bashrc
source ~/.bashrc

```
